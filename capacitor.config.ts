import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app-compartilhamento-calcados',
  webDir: 'www'
};

export default config;
